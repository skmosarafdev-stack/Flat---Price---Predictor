# AI-Based Flat Price Prediction System
Academic project using 100 synthetic flat records and Random Forest Regression.

Run:
1. `pip install -r requirements.txt`
2. `python train_model.py`
3. `python app.py`
4. Open the Flask URL shown in the terminal.

The included data is synthetic/demo data and must not be used for real property valuation.
